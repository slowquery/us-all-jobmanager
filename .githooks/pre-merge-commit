#!/usr/bin/env bash
set -euo pipefail
branch="$(git symbolic-ref --short HEAD 2>/dev/null || echo DETACHED)"
if [ "$branch" = "master" ]; then
  echo "governance/pre-merge-commit: merge commits on 'master' are refused locally. Squash-merge via PR on GitHub (Rule 7)." >&2
  exit 1
fi
exit 0
