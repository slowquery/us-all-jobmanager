<!-- Do not edit — this is a mirror of AGENTS.md (Rule 8). Edit AGENTS.md instead. -->
# UsAllJobManager — Agent Operating Rules

@AGENTS.md

<!--
Sync note: this file intentionally imports AGENTS.md via the `@AGENTS.md` line above so the
rules never drift. If a Claude Code version does not expand `@`-imports, replace this file with
a verbatim copy of AGENTS.md and add a release-checklist diff step (see logs/ Step-0 0a).
Normative source of truth for every rule remains .gjc/rules/*.md.
-->
